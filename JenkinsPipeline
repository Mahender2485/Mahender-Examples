pipeline{
    agent{
        stages{
            stage('Checkout') {
                step{
                    Checkout scm
                }
            }
            stage('Install Dependencies') {
                step{
                    sh 'npm install'
                }
            }
            stage('Run Tests') {
                step{
                    sh 'npm test'
                }
            }
        }
        post {
            always {
            junit 'test-results/*.xml'
            }
        }   
    }
}